import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  // Primary colors
  static const primary = Color(0xFF006B5F);
  static const primaryVariant = Color(0xFF00463E);
  static const secondary = Color(0xFF00A087);
  static const secondaryVariant = Color(0xFF007F67);
  
  // New gradient colors for design
  static const gradientPurpleStart = Color(0xFF8B5CF6);
  static const gradientPurpleEnd = Color(0xFF6D28D9);
  
  // Feature button neon
  static const neonYellow = Color(0xFFCBFF00);
  static const neonGreen = Color(0xFF00FF88);
  
  // Backgrounds and surfaces
  static const background = Color(0xFFF5F7F8);
  static const surface = Colors.white;
  
  // Status colors
  static const error = Color(0xFFB00020);
  static const success = Color(0xFF22C55E);
  static const warning = Color(0xFFFB923C);
  
  // Text colors
  static const textPrimary = Color(0xFF17201F);
  static const textSecondary = Color(0xFF6B7575);
  
  // Utility colors
  static const border = Color(0xFFE2E8ED);
  static const inputFill = Color(0xFFF2F5F6);
  static const divider = Color(0xFFE8EAED);
}
